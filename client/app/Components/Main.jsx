import React from 'react';


const Main = (props) => {
  return (
    <div>
      <button onClick={/* drop down menu componenet renders, animates */}>Menu hamburger</button>
      <button onClick={/* starts new session */}> Create new session </button>
      I am the Main page!
    </div>
  )
}

export default Main;
