const express = require('express');
const mongoose = require('mongoose');
var config = require('../db/config.js'); // run mongod

var router = express.Router();

// authentication

// comment fetching and creating

module.exports = router;
